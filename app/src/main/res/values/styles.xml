<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Estilo básico para os botões -->
    <style name="ButtonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:background">@drawable/rounded_button</item> <!-- Usando um drawable para a borda arredondada -->
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:elevation">4dp</item> <!-- Sombras para dar um efeito 3D -->
    </style>

    <!-- Estilo para botões de operação matemática -->
    <style name="OperationButtonStyle" parent="ButtonStyle">
        <item name="android:background">@drawable/rounded_button_operation</item> <!-- Botão especial para operações -->
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- Estilo para botões de funções, como C e = -->
    <style name="FunctionButtonStyle" parent="ButtonStyle">
        <item name="android:background">@drawable/rounded_button_function</item> <!-- Botão especial para funções -->
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>

